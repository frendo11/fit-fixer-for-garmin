// sockets/socketHandler.js

module.exports = (socket, io) => {
    // Additional Socket.io event handlers can be added here
    // For this project, we'll handle progress updates in the fileProcessor
};
